/**
 * Created by Gebruiker on 23-6-2017.
 */
import {Model} from 'backbone';

/**
 * @desc model for every Expedition in the collection
 *
 * @constructor
 */
const Expedition = Model.extend({});

export default Expedition;
